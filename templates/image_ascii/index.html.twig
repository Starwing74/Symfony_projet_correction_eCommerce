{{ form_start(form) }}
    {{ form_rest(form) }}
    <input type="submit" value="Valider" />
{{ form_end(form) }}

<div style="font-size: 1px;">
    {% for col in data %}
        {% for char in col %}
            {{ char }}
        {% endfor %}
        <br />
    {% endfor %}
</div>
