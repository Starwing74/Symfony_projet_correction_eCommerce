{% extends 'base.html.twig' %}

{% block title %}
    {% if isAdd %}
        Ajout d'une catégorie
    {% else %}
        Modification d'une categorie {{ category.name }}
    {% endif %}
{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_rest(form) }}
    <input type="submit" value="Valider !" />
    {{ form_end(form) }}
{% endblock %}
